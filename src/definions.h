#ifndef DEFINIONS_H
#define DEFINIONS_H

#include <string>
#include <vector>

using std::string;
using std::vector;

using Buffer = vector<char>;

#define MAX_BUFFER_SIZE 4096
#define DEFAULT_BUFFER_SIZE 256

#endif // DEFINIONS_H
